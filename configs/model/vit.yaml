class_name: models.nets.t2t.T2TViT
input_size: 32 
stem:
  class_name: models.stems.patch.T2TPatch
  params:
    kernel_sizes: [3, 3, 3]
    strides: [2, 2, 2]
neck:
  class_name: models.necks.patch.SequencePooling
head:
  class_name: models.heads.fc.FullyConnectedHead
  params:
    layer_units: 
      - ${datamodule.dataset.n_classes}
    activations: ["id"]
    dropout_rates: [0.0]
